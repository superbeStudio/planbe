import { GOAL, GOAL_QUERY_KEY } from "./goal";
import { CATEGORY, CATEGORY_QUERY_KEY } from "./category";

export const API = {
  CATEGORY,
  GOAL,
};

export const QUERY_KEY = { CATEGORY: CATEGORY_QUERY_KEY, GOAL: GOAL_QUERY_KEY };
